# Springboot 에서는 test 에 application.properties 가 정의 되어 있지 않을 시 
# main 에 존재하는 application.properties 설정을 가져와서 그대로 적용을 시켜준다

# 만약 test 에도 application.properties 설정을 따로 원할 시 
# src/test/resource/ 경로에 application.properties 파일을 생성 해준다.

# TEST
# spring.profiles.active=local
# Local 환경
# spring.profiles=local
spring.jpa.show-sql=true




